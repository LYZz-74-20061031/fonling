# Fonling 项目交接文档

更新时间：2026-08-23  
项目目录：`D:\github\fonling`  
主分支：`main`

## 1. 项目定位

Fonling 是一个主要面向 Chrome 手机主屏幕使用的自用 AI 剧情聊天网页，产品名称为“智能剧情故事”。项目是无构建步骤的静态网页：通过 URL 打开 `index.html`，运行时数据保存在浏览器 `localStorage`。

项目采用单线剧情，不计划加入剧情分支或存档点。开发优先级是：功能正确性 > 剧情玩法 > 外观。

## 2. 当前可运行主干

运行相关文件：

- `index.html`：页面主体、角色系统、聊天流程、设置、导入导出及应用级协调逻辑。
- `manifest.webmanifest`：PWA 元数据。
- `icons/`：192 和 512 图标。
- `css/memory.css`：记忆中心样式。
- `css/model.css`：模型设置、付费回复标记和输入区模型控件样式。
- `js/memory/`：结构化记忆、场景、分析、上下文、存储、控制器和 UI。
- `js/model/`：全局模型配置、请求网关和一次性模型会话状态。

项目无需 npm 安装或打包，可由任意静态 HTTP 服务托管。不要直接把 API Key 写入源码。

## 3. 已完成功能

### 3.1 角色与聊天

- 支持创建多个智能体并切换。
- 支持 AI 人设、回复风格、用户身份和聊天背景。
- 支持主控及多个用户角色身份切换。
- 输入区左侧显示“深度思考”，右侧显示当前身份；发送按钮统一为白底黑色图标。
- 支持流式回复、编辑最近 AI 回复、重新深度思考、清空聊天和单智能体 JSON 导入导出。
- 清空聊天会删除原始消息、滚动摘要以及由对话派生的候选记忆/分析痕迹，但保留人设、角色、长期记忆和当前场景等设置。

### 3.2 结构化记忆中心

记忆中心已经独立，并将记忆分为：

- 历史事件。
- 关键信息。
- 当前场景。

当前场景为结构化字段，包括时间、地点、在场角色、当前目标、当前冲突、人物状态、环境和补充说明。

AI 可以在对话后提出记忆候选，但所有候选都必须由用户处理。用户可以确认、修改后确认或不计入，也可以主动写入记忆。未处理候选通过“发现了新的记忆”提示进入记忆中心。

主要数据字段包括：

- `memorySchemaVersion`（当前为 2）。
- `memories`。
- `currentScene`。
- `memoryCandidates`。
- `memoryAnalysis`。
- `memoryRequestTraces`。

相关实现位于 `js/memory/`。修改聊天回溯、清空、重新生成或导入逻辑时，必须同步处理候选记忆、分析进度和消息来源痕迹，避免 AI 继续记住已经删除的对话。

### 3.3 模型路由与成本控制

模型配置是全局配置，不属于某一个智能体，存储键为：

```text
fonling_model_config_v1
```

当前策略：

- 普通剧情默认使用 `glm-4.7-flash`。
- 自动摘要固定使用 `glm-4.7-flash`。
- 记忆分析固定使用 `glm-4.7-flash`。
- 输入区“深度思考”只影响下一条发送，使用 `glm-4.5-air`，请求完成后自动恢复免费模型。
- 最新 AI 回复的“重新深度思考”固定使用 `glm-4.5-air`。
- GLM 请求明确发送 `thinking: { type: 'disabled' }`。
- 免费 GLM 仅在网络错误、429 或 5xx 时自动重试一次。
- Air 不自动重试，避免重复付费。
- DeepSeek 只作为用户手动选择的备用服务，不做自动降级。
- 只有 Air 生成的回复显示付费来源标记。

核心文件：

- `js/model/model-config.js`：默认配置、校验、持久化以及旧 DeepSeek Key 的一次性迁移。
- `js/model/model-gateway.js`：GLM/DeepSeek 请求体、SSE、错误分类和重试策略。
- `js/model/model-session.js`：一次性 Air 状态、并发保护和回复来源元数据。
- `css/model.css`：模型设置和对话区 UI。

设置界面提供全局 GLM Key、免费模型连接测试、默认剧情模型选择，以及折叠的 DeepSeek 备用设置。API Key 不随角色 JSON 导出。旧 JSON 如果包含 DeepSeek Key，只会在全局 DeepSeek Key 为空时进行一次迁移。

### 3.4 已实现且不能回归的底层约束

历史规格文件删除后，以下内容视为必须继续遵守的设计决策：

- 滚动摘要在原始消息超过 30 条时触发，成功后保留最近 14 条原始消息；摘要输出上限为 1024 tokens。
- 每次摘要必须把旧摘要与本次退役消息合并重写为一份有界摘要，不能无限追加摘要片段。
- 摘要请求失败或返回空内容时，必须保留旧摘要和所有原始消息。
- 角色改名时，如果被改名角色正处于选中状态，必须同步更新 `currentRole`；历史消息中的 `_roleName` 也必须同步更新，以保持身份与气泡颜色连续。
- SSE 解析必须使用流式 `TextDecoder`，正确处理中文或 emoji 被拆在不同字节块、最后一帧没有换行以及模型片段中的有效空格；发送和重新生成必须共用等价的完整解析行为。
- PWA 使用静态 `manifest.webmanifest` 和 `icons/icon-192.png`、`icons/icon-512.png`，应用名称保持“智能剧情故事”，不要恢复运行时 Blob manifest。
- 正式记忆类型为 `history_event` 和 `key_info`，每个智能体只有一份结构化 `currentScene`。
- AI 候选操作包括 `add`、`update`、`merge`、`resolve` 和 `scene_patch`；解决类状态使用 `archived`、`resolved` 或 `invalidated`，冲突内容不得自动覆盖正式记忆。
- 每轮自动分析最多产生 3 条候选，只能在剧情回复完整生成并成功持久化后开始；任何解析或请求失败都不能修改正式记忆和当前场景。
- 常驻记忆预算为 4000 字符，结构化记忆总上下文预算为 8000 字符。当前场景、常驻记忆、直接相关记忆和近期记忆按优先级进入请求。
- 剧情上下文顺序应保持为：AI 人设/世界观 → 用户身份/回复风格/当前角色 → 当前场景 → 常驻记忆 → 相关普通记忆 → 滚动摘要 → 最近原始消息 → 本轮用户消息。
- `resolved` 和 `invalidated` 的关键信息默认不发送；`archived` 历史事件降低优先级，但直接相关时仍可选入。
- 每次剧情请求保留使用过的记忆 ID、场景版本和摘要使用情况，使“查看本次使用的记忆”可追踪。
- 数据迁移、JSON 导入和双键保存必须先在临时对象中完成校验，再原子替换；失败时恢复原始角色状态和原始存储字节。

## 4. 最近验收结果

在删除开发测试目录之前，仓库的 Node 回归测试结果为：

```text
284 项通过，0 项失败
```

同时完成了以下验收：

- `index.html` 内嵌 JavaScript 语法检查通过。
- 三个 `js/model/` 模块语法检查通过。
- `git diff --check` 通过。
- Chrome 风格浏览器桌面视口验收通过。
- 390 × 844 手机视口无横向溢出。
- 设置区、一次性 Air 按钮、当前身份显示和白底黑色发送按钮显示正常。
- 浏览器控制台没有相关错误或警告。
- 用户亲自验证 `glm-4.7-flash` 与 `glm-4.5-air` 均能真实调用。
- 用户确认 Air 调用确实消耗智谱赠送的 Air Token，说明路由正确。
- 源码中未写入用户提供的 GLM API Key。

按用户要求，交接时删除了 `tests/`、开发规格/计划、临时界面草稿和旧 worktree，因此后续如果要继续自动化测试，应重新建立测试目录；不要把“当前没有测试文件”误判为从未验证。

## 5. 数据与安全约束

- 所有角色数据和全局模型配置都保存在浏览器本地；清理浏览器站点数据会造成数据丢失。
- 用户接受在本地设置中明文保存 API Key，但不允许将真实 Key 写入源码或交接文档。
- 导出 JSON 是“一份 JSON 对应一个智能体”，这是期望行为，不要擅自改成全角色打包。
- 当前导出不包含全局 API Key 和模型配置。
- 修改保存逻辑时应保持原子写入/失败回滚，避免角色设置和消息只保存一半。
- 实现破坏性操作前，优先保留可导出的数据备份能力。

## 6. 推荐的下一项工作：章节系统 v1

下一阶段建议先设计、后实现章节系统。用户已经确认：

- 只需要单线剧情。
- 不需要存档点或剧情分支。
- 章节系统应与结构化记忆、当前场景和上下文压缩联动。
- 新功能应先完成 brainstorming 和规格确认，不要一上来直接改代码。

建议第一版讨论这些问题：

1. 章节如何手动开始、结束和改名。
2. 章节结束摘要如何由免费 GLM 生成，并由用户确认或修改。
3. 原始消息是否永久完整保留，以及章节和消息之间如何关联。
4. 已结束章节向模型提供多少摘要，当前章节保留多少近期原文。
5. “上回剧情”、未解决冲突和章节结束时场景快照如何展示。
6. 章节数据如何加入单智能体 JSON 导入导出及版本迁移。

推荐的上下文层次：

```text
AI 人设与回复风格
→ 长期结构化记忆
→ 已结束章节摘要
→ 当前结构化场景
→ 当前章节近期对话
```

章节功能的重要目标之一是减少长剧情对上下文 Token 的占用，而不牺牲连续性。

## 7. 新对话开始时的检查顺序

1. 阅读本文件。
2. 运行 `git status --short`，确认用户是否已经提交交接清理。
3. 阅读 `index.html`、`js/memory/`、`js/model/` 和相关 CSS，不要仅依据本文猜测当前代码。
4. 在设计章节系统前，先确认现有角色 JSON 结构和本地存储迁移方式。
5. 先与用户完成章节系统规格确认，再创建实现计划和测试。
6. 不要提交或推送 Git；用户会通过 GitHub Desktop 完成提交。
